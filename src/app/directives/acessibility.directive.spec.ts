import { AcessibilityDirective } from './acessibility.directive';

describe('AcessibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new AcessibilityDirective();
    expect(directive).toBeTruthy();
  });
});
